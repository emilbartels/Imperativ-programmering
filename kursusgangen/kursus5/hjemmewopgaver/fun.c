#include <math.h>
#include <stdio.h>

int main(void){
int x,v,w, y;
int result;

// a:
//Brug af sqrt og så pow(x, n) for at få x^n
result = sqrt(x+v) * pow(w, 2);

// b:
//Brug af log(x) for naturlige logaritme
result = log(pow(x, y));

// c:
//Brug af sqrt og så pow(x, n) for at få x^n
result = sqrt(pow((x-y),3));

// d:

}

